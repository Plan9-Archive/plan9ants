--- /sys/src/9/port/auth.c	Thu Mar 10 17:52:45 2016
+++ ../frontmods/port/auth.c	Mon Dec 18 10:56:51 2017
@@ -141,6 +141,7 @@
 
 	renameuser(eve, buf);
 	srvrenameuser(eve, buf);
+	zrvrenameuser(eve, buf);
 	shrrenameuser(eve, buf);
 	kstrdup(&eve, buf);
 	kstrdup(&up->user, buf);
