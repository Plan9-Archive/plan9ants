at startup enter bootfile=9ants then type boot, use 9pcf if the new kernel fails
use bootcmd=plan9rc for configurable bootup
listens on port 17060 with password rootless by default
do not use default configuration on a public unsecured network

to enter the 9front userland open a window and

rerootwin -f boot
service=con
. $home/lib/profile
grio
